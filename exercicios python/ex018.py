import math
ang = float(input('digite um angulo: '))

print('o cosseno sera: {:.2f}'.format(math.cos(math.radians(ang))))
print('o seno sera: {:.2f}'.format(math.sin(math.radians(ang))))
print('a tangente sera: {:.2f}'.format(math.tan(math.radians(ang))))